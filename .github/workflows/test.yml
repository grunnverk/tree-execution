name: Run Tests

on:
  push:
    branches:
      - main
      - working
      - 'release/**'
      - 'feature/**'
      - 'dependabot/**'
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Install dependencies
        run: |
          # Use public registry (package-lock.json is gitignored to avoid Walmart registry refs)
          npm config set registry https://registry.npmjs.org/
          npm install --verbose --foreground-scripts
        timeout-minutes: 10
      - run: npm run lint
      - run: npm run build
      - run: npm run test
